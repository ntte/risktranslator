# risktranslator
MVP: take an “assessment output” (start with plain text / JSON), convert it into:  Structured findings JSON  CISO brief (markdown)  CTO brief (markdown)  Once that works end-to-end, we’ll add:  PDF parsing  slide/PDF export  RAG “approved messaging” library  simple UI
